import React from "react";
import { GripRightContainer } from "./elements";
export const GripRight = ({ bottomPosition, children, isMaximized, topPosition, }) => (<GripRightContainer topPosition={topPosition} bottomPosition={bottomPosition} isMaximized={isMaximized}>
    {children}
  </GripRightContainer>);
//# sourceMappingURL=index.jsx.map