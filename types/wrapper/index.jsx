import React from "react";
import { WrapperContainer } from "./elements";
export const Wrapper = ({ children, containerStyle, minHeight, }) => (<WrapperContainer css={containerStyle} minHeight={minHeight}>
    {children}
  </WrapperContainer>);
//# sourceMappingURL=index.jsx.map